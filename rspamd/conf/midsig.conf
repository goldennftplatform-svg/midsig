# /etc/rspamd/local.d/midsig.conf
# MIDSIG verification module settings.
midsig {
  # require postage of at least N leading zero bits (0 = signatures only)
  required_bits = 0;
  # domains allowed to send unsigned (comma-ish ucl list)
  exempt_domains = [];
}
